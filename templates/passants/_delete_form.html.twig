<form method="post" action="{{ path('passants_delete', {'id': passant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ passant.id) }}">
    <button class="btn-danger">Delete</button>
</form>
