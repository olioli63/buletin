{% extends 'base.html.twig' %}

{% block title %}Edit Passants{% endblock %}

{% block body %}
    <h1>Edit Passants</h1>

    {{ include('passants/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('passants_index') }}">back to list</a>

    {{ include('passants/_delete_form.html.twig') }}
{% endblock %}
