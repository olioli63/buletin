<form method="post" action="{{ path('fiche_inscription_delete', {'id': fiche_inscription.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fiche_inscription.id) }}">
    <button class="btn-danger">Delete</button>
</form>
