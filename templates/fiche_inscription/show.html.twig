{% extends 'base.html.twig' %}

{% block title %}FicheInscription{% endblock %}

{% block body %}
    <h1>FicheInscription</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fiche_inscription.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ fiche_inscription.Date ? fiche_inscription.Date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Remarque</th>
                <td>{{ fiche_inscription.Remarque }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fiche_inscription_index') }}">back to list</a>

    <a href="{{ path('fiche_inscription_edit', {'id': fiche_inscription.id}) }}">edit</a>

    {{ include('fiche_inscription/_delete_form.html.twig') }}
{% endblock %}
